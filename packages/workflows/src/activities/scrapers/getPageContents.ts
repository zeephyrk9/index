export async function getPageContents() {
    // @todo
    // Implement. This function must return some HTML wrapper, that
    // is easily manipilated (something like https://www.npmjs.com/package/cheerio) 
    return;
};